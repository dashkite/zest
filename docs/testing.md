# Testing

This document outlines the testing approach for the Zest repository.

## Executing Tests

To run the test suite, use the `genie` task manager:

```bash
npx genie test
```

If the task manager fails or the command is not found, you can fall back to directly running the internal test script:

```bash
./scripts/test
```
